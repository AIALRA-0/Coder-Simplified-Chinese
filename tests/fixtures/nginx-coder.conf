# Public dry-run fixture; coder.example is a reserved documentation domain.
server {
    listen 443 ssl;
    server_name coder.example;

    location / {
        proxy_set_header Host $host;
        proxy_pass http://127.0.0.1:7080;
    }
}
